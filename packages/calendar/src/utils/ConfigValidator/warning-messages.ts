import { WarningCodes } from './warning-codes';

export const WARNING_MESSAGES = {
  [WarningCodes.TIMEZONE_LIMIT_EXCEEDED]:
    'Timezones are limited to a three at most. Every timezone above that limit will not be used.',
};
