---
title: Overview
sidebar_position: 1
---

import TryOnCodePen from '@site/src/components/TryOnCodePen';

# Props

`<MhCalendar>` (`<mh-calendar>` as a plain custom element) takes three top-level props:

| Prop | Type | Description |
|---|---|---|
| `config` | `IMHCalendarFullOptions` | View type, behavior toggles, styling, business hours — everything except the events themselves. Documented field-by-field below. |
| `events` | `IMHCalendarEvent[]` | The events to render. See [Events](/docs/props/events). |
| `reactComponent` | `any` | Present in the type signature but has no documented purpose — don't set it. |

`config` is a single flat object, merged internally from three layers (base behavior,
multi-view/grid options, week/shiftplan-specific options) — you never nest anything, just pass one
object with whichever fields you need.

<TryOnCodePen topic="basic-usage" />

```tsx
<MhCalendar config={{ viewType: 'MONTH', avaliableViews: ['MONTH', 'WEEK', 'AGENDA'] }} events={events} />
```

### Typo heads-up

:::info
The real, current field name for restricting available views is **`avaliableViews`** (missing
"il") — this isn't a typo in these docs, it's the actual API. Every field name on this page and its
children is copied from the shipped TypeScript types.
:::

## Every `config` field

### Behavior & navigation

- [View type & available views](/docs/props/view-type) — `viewType`, `avaliableViews`
- [Start date](/docs/props/start-date) — `startDate`
- [Navigation visibility](/docs/props/navigation-visibility) — `showDateSwitcher`, `showViewTypeSwitcher`, `showCalendarNavigation`, `showViewHeader`
- [Dragging & resizing](/docs/props/event-drag-resize) — `allowEventDragging`, `allowEventResize`, `minEventDuration`
- [Create event on click](/docs/props/create-event-on-click) — `createEventOnClick`
- [Virtual scroll](/docs/props/virtual-scroll) — `fixedHeight`, `virtualScrollHeight`

### Look & content

- [Theme](/docs/props/theme) — `theme`
- [Style](/docs/props/style) — `style`
- [Custom event rendering](/docs/props/event-content) — `eventContent`, `eventSmallContent`

### Callbacks

- [Click callbacks](/docs/props/click-callbacks) — `onEventClick`, `onRightEventClick`, `onDayClick`, `onRightDayClick`
- [Create & update callbacks](/docs/props/event-lifecycle) — `onEventCreated`, `onEventUpdated`

### Time grid (Day/Week/Month/Shiftplan)

- [Displayed time range](/docs/props/time-range) — `showTimeFrom`, `showTimeTo`
- [Slot interval & hour labels](/docs/props/slot-interval) — `slotInterval`, `hoursSlotInterval`, `hoursDisplayFormat`
- [All-day row](/docs/props/all-day-row) — `showAllDayTasks`, `allDayEventsHeight`
- [Sticky day headers](/docs/props/sticky-days) — `makeAllDaysSticky`
- [Hidden days](/docs/props/hidden-days) — `hiddenDays`
- [Business hours](/docs/props/business-hours) — `businessHours`, `blockBusinessHours`
- [Timezones](/docs/props/timezones) — `timezones`, `timezoneLabel`
- [Event display mode](/docs/props/event-display-mode) — `eventDisplayMode`
- [Current-time indicator](/docs/props/show-time-indicator) — `showTimeIndicator`

### Shiftplan

- [Resources](/docs/props/resources) — `resources`
- [Shiftplan days](/docs/props/shiftplan-days) — `shiftplanDays`, `customWeekView`, `showWeekends`

## Reading state back out

`config` and `events` are one-way — they describe what the calendar should render. To read the
calendar's current state or imperatively navigate/mutate events, see
[Reading state with `getApi()`](/docs/guides/reading-state).
