---
"@mhcalendar/react": patch
---

Fix the `MhCalendarMoreEventsIndicator` wrapper missing the `hiddenCount` prop type, and use an
explicit `.js` extension in the internal re-export to fix ESM resolution.
